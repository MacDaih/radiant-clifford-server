# Radiant Clifford Server

Backend server component of Radiant Clifford project. The latter consists of weather data collected on a raspberry pi and sending messages to the server via an MQTT v5 broker.
The server provides a reporting API.

# Radiant Clifford Server

Backend server component of Radiant Clifford project. The latter consists of a weather data collected on a raspberry pi and sending messages to the server via an MQTT v5 broker.
